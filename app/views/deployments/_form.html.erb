<%= form_for @deployment do |f| %>
  <%= base_errors_for @deployment %>
  <%= text_f f, :name %>
  <%= select_f f, :stack_id, Stack.all, :id, :name %>

  <%= render "settings", :f => f unless @deployment.new_record? %>
  <%= submit_or_cancel f %>
<% end %>
