<%= text_f f, :quantity, :size => 'col-xs-3', :title => "Amount of instances to manage",
           :value              => resource_quantity(f.object.quantity),
           :label              => f.object.resource.name %>
